public enum Orientation
{
    Left,
    Center,
    Right
}

public enum PlayerType
{
    PlayerOne,
    PlayerTwo
}

public enum MoveKey
{
    Left,
    Right,
    Trap,
    Jump
}

public enum GameMode
{
    Instructions,
    Break,
    CutScene,
    Repair,
    GameOver,
    None,
    Stop
}

public enum PlayerState
{
    Run,
    Jump
}