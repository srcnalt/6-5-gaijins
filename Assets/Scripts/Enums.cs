public enum Orientation
{
    Left,
    Center,
    Right
}

public enum PlayerType
{
    PlayerOne,
    PlayerTwo
}

public enum MoveKey
{
    Left,
    Right,
    Trap
}

public enum GameMode
{
    Instructions,
    Break,
    CutScene,
    Repair,
    GameOver,
    None,
    Stop
}