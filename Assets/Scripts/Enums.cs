public enum Orientation
{
    Left,
    Center,
    Right
}

public enum PlayerType
{
    PlayerOne,
    PlayerTwo
}

public enum MoveKey
{
    Left,
    Right
}

public enum GameMode
{
    Break,
    CutScene,
    Repair,
    GameOver,
    None,
    Stop
}